# Generated asset kind taxonomy

`generated_assets[].kind` will start with a small closed vocabulary: `agent_instructions`, `skill`, `documentation`, `template_config`, `project_plan`, and `metadata`. This is enough to distinguish runtime agent assets, docs, generator configuration, planning artifacts, and generator-owned metadata without overfitting categories before upgrade behavior exists.
