# Project generator as primary product

`repo-familiar` will be primarily a project generator for downstream repositories, with this repository serving as the canonical reference source. We considered treating it as a personal reference repo or workstation dotfiles installer, but chose project generation because it keeps the design focused on repeatable bootstrap, selectable agent/model profiles, and documentation scaffolds rather than general machine setup.
