# Existing repository bootstrap

`repo-familiar` will support bootstrapping existing downstream repositories as a second mode alongside generating new repositories. The existing-repository flow should audit first, preview changes, and add selected tools, skills, model profiles, agent instructions, documentation scaffolds, and bootstrap metadata without overwriting user-owned files by default.
