# Named model profile schema

`.agents/models.yml` will use named profiles as stable handles, with pragmatic fields such as `provider`, `model`, `use`, `harnesses`, and `notes`. Provider secrets are excluded, and provider metadata will not be over-normalized until real generator templates need the extra structure.
